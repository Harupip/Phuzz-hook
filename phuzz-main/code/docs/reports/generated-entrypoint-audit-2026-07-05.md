# Generated Entrypoint Audit - 2026-07-05

Scope: live runtime capture and generated hook configs for WordPress entrypoints. This audit checks the current checkout before new implementation work.

| Entrypoint type | Runtime capture | direct_http_candidate classification | Generated config | callback_reached validation | Related files/functions | Missing for e2e |
| --- | --- | --- | --- | --- | --- | --- |
| `wp_ajax_*` | Yes, via normal hook registration artifacts. | Yes. `direct_http_details()` maps to `POST /wp-admin/admin-ajax.php` with fixed `action`. | Yes, live seed generator and exporter support it. | Yes, generated config runner checks request artifacts via `evaluate_artifact_payloads()`. | `hook_energy/entrypoints.py::direct_http_details`, `seed_generation/generator.py::LiveHookSeedGenerator._generate_seed_template`, `seed_generation/config_exporter.py::build_config_for_seed_item`, `seed_generation/generated_config_runner.py::run_generated_configs`. | Existing support. |
| `wp_ajax_nopriv_*` | Yes, via normal hook registration artifacts. | Yes, longer nopriv prefix is checked before authenticated AJAX. | Yes. | Yes. | Same as AJAX above; guarded by `tests/test_entrypoints.py` and `tests/test_seed_to_config_exporter.py`. | Existing support. |
| `admin_post_*` | Yes, bootstrap probes include `/wp-admin/admin-post.php?action=hookphuzz_probe`; hook registrations are captured as normal callbacks. | Yes. `direct_http_details()` maps to `POST /wp-admin/admin-post.php` with fixed `action`. | Yes, live seed generator allows `admin_post_*`, and generated configs already exist under `configs/generated-hooks/`. | Yes, through generated config runner; e2e depends on plugin context/auth branch. | `entrypoints.py::_ADMIN_POST_RULES`, `bootstrap_probe_runner.py::default_probes`, `generator.py::_generate_seed_template`, `config_exporter.py`. | Add richer metadata/report fields only. |
| `admin_post_nopriv_*` | Yes, same runtime hook registration path as `admin_post_*`. | Yes. | Yes. | Yes, same runner path. | Same as admin-post above. | Add richer metadata/report fields only. |
| REST `register_rest_route()` | Yes. UOPZ wraps `register_rest_route()` and records `entrypoint_type=rest_route`, namespace, route, methods, callback, and permission callback. | Yes when runtime metadata is first-class REST. `direct_http_details()` maps to `/wp-json/<namespace>/<route>`. | Yes. Exporter preserves `entrypoint_type=rest_route`; no `action` param. | Yes. Runner preserves `entrypoint_type` and checks callback ID. | `web/instrumentation/hook_coverage/uopz_hook_wp.php::__uopz_register_rest_route`, `entrypoints.py::rest_http_template`, `tests/test_bootstrap_entry_discovery.py`, `tests/test_seed_generation_with_input_params.py`, `tests/test_generated_config_runner.py`. | Regex route placeholders are not concretized into path fuzz params. |
| `login_form_*` | Partial. Hooks are visible as normal registered callbacks when reached by bootstrap login probe. | Yes. `direct_http_details()` maps to `POST /wp-login.php` with fixed `action` in query params. | No in live seed generator: `_generate_seed_template()` currently only allows AJAX, admin-post, and REST. | No generated-config e2e path yet. | `entrypoints.py::_LOGIN_FORM_RULES`, `entry_classifier.py`, `tests/test_entry_classifier.py`. | Allow live seed generation, add contract tests, preserve metadata. |
| `heartbeat_received` | Partial. Bootstrap probes do not send heartbeat itself; callback can be captured when hook registration is observed. | Yes, exact hook maps to `POST /wp-admin/admin-ajax.php` with `action=heartbeat`. | No in live seed generator: heartbeat is blocked by the current allowlist. | No generated-config e2e path yet. | `entrypoints.py::DIRECT_HTTP_EXACT_RULES`, `entry_classifier.py`, `tests/test_entrypoints.py`. | Allow live seed generation and add auth metadata. |
| `heartbeat_nopriv_received` | Partial. Same capture path as heartbeat; nopriv branch depends on request context. | Yes, exact hook maps to `POST /wp-admin/admin-ajax.php` with `action=heartbeat` and unauth auth mode. | No in live seed generator. | No generated-config e2e path yet. | Same as heartbeat above. | Allow live seed generation and mark `auth_mode=nopriv`/unauth-capable. |
| `xmlrpc_methods` | Partial. Bootstrap probes call `/xmlrpc.php` with `system.listMethods`; classifier can identify method-map records when runtime data includes `method_map`, `xmlrpc_method`, or `methods`. | No direct HTTP candidate; classified as setup-required/manual. | No, and should stay manual unless a concrete XML-RPC method name/body is known. | No generated-config validation. | `bootstrap_probe_runner.py::XMLRPC_DEMO_BODY`, `entry_classifier.py::_has_xmlrpc_record`. | Runtime method-name extraction plus minimal XML-RPC body generation. Until then: `manual_analysis`, `fuzzing_ready=false`. |
| shortcode | Partial. Classifier detects shortcode-shaped records, and input extractor can read shallow shortcode defaults from source, but runtime does not wrap `add_shortcode()` as a first-class entrypoint. | No direct HTTP candidate; setup-required/manual because it needs content setup. | No. | No. | `entry_classifier.py::_setup_required_details`, `seed_generation/input_extractor.py`, `tests/test_input_signature_extractor.py`. | Runtime `add_shortcode()` capture plus page/post setup workflow. Until then: `manual_analysis`, `fuzzing_ready=false`. |

Implementation target after audit:

- Keep existing Priority 1 support and add only missing metadata/report fields.
- Enable Priority 2 live seed generation for `login_form_*` and heartbeat hooks through the existing `entrypoints.py` templates.
- Keep Priority 3 XML-RPC and shortcode in manual analysis until runtime capture has enough data to build correct requests.
