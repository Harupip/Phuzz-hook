# Plugin Matrix Reports

This folder stores real WordPress plugin validation outputs generated by:

```powershell
.\run-wordpress-plugin-matrix.ps1 -DownloadMissing
```

Each run can produce:

- `*.md`: human-readable success/failure summary
- `*.json`: machine-readable result list
- `*.console.log`: captured console output when saved manually

The runner writes new reports here by default.
