# Phase 13

`bash research/hookphuzz-opcode/phase13/run.sh` runs the pinned local plugin matrix. It captures the local WordPress REST registry through the existing UOPZ hook, generates only schema-compatible PHUZZ configs, and records redacted replay evidence.
